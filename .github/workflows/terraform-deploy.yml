name: Deploy Infrastructure (Terraform)

on:
  push:
    branches: [main]
    paths:
      - "api/**"
      - "infra/**"
      - "Dockerfile"
      - ".github/workflows/terraform-deploy.yml"
  workflow_dispatch:

# Required for Workload Identity Federation
permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    env:
      GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
      GCP_PROJECT_NUMBER: ${{ secrets.GCP_PROJECT_NUMBER }}
      GCP_REGION: ${{ secrets.GCP_REGION }}
      SERVICE_NAME: skane-trails-api
      REPOSITORY: skane-trails
      TF_DIR: infra/environments/dev

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Authenticate to Google Cloud
        id: auth
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3
        with:
          workload_identity_provider: projects/${{ env.GCP_PROJECT_NUMBER }}/locations/global/workloadIdentityPools/st-github-actions/providers/github-provider
          service_account: st-gh-actions-terraform@${{ env.GCP_PROJECT_ID }}.iam.gserviceaccount.com

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@aa5489c8933f4cc7a4f7d45035b3b1440c9c10db # v3
        with:
          project_id: ${{ env.GCP_PROJECT_ID }}

      # ---------------------------------------------------------------
      # Reconstruct gitignored files from GitHub Secrets
      # ---------------------------------------------------------------

      - name: Reconstruct access files and tfvars
        working-directory: ${{ env.TF_DIR }}
        run: |
          mkdir -p access
          printf '%s\n' "${{ secrets.TF_SUPERUSERS }}" > access/superusers.txt
          printf '%s\n' '${{ secrets.TF_VARS_FILE }}' > terraform.tfvars

          # Partial backend config — actual bucket/prefix come from -backend-config flags
          printf 'terraform {\n  backend "gcs" {}\n}\n' > backend.tf

          # Mask each line to avoid leaking PII in Terraform plan/apply output
          for f in access/superusers.txt terraform.tfvars; do
            if [ -f "$f" ]; then
              while IFS= read -r line; do
                if [ -n "$line" ]; then
                  echo "::add-mask::$line"
                fi
              done < "$f"
            fi
          done

      # ---------------------------------------------------------------
      # Docker image build + push
      # ---------------------------------------------------------------

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.GCP_REGION }}-docker.pkg.dev --quiet

      - name: Build and push Docker image
        run: |
          IMAGE="${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:${{ github.sha }}"
          LATEST="${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:latest"
          docker build -t "$IMAGE" -t "$LATEST" .
          docker push "$IMAGE"
          docker push "$LATEST"

      # ---------------------------------------------------------------
      # Terraform apply
      # ---------------------------------------------------------------

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3.1.2
        with:
          terraform_version: "~> 1.12"

      - name: Terraform init
        working-directory: ${{ env.TF_DIR }}
        run: |
          terraform init -input=false \
            -backend-config="bucket=${{ secrets.TF_BACKEND_BUCKET }}" \
            -backend-config="prefix=${{ secrets.TF_BACKEND_PREFIX }}"

      - name: Terraform apply
        working-directory: ${{ env.TF_DIR }}
        run: terraform apply -auto-approve -input=false -var "image_tag=${{ github.sha }}"
